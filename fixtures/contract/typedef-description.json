{
  "name": "typedef-description",
  "description": "typedef with a trailing - description: split into doc, body stays clean (reuses the param - convention).",
  "source": "// T: typedef User = { id: string, name: string } - A registered user\n",
  "expected": [
    {
      "nodeStartOffset": 0,
      "etyStartOffset": 0,
      "etyEndOffset": 69,
      "kind": "typedef",
      "name": "User",
      "ety": "{ id: string, name: string }",
      "doc": "A registered user"
    }
  ]
}
